import 'package:flutter/material.dart';

class HnH extends StatelessWidget {
  const HnH({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("hhhhhhhhhhh"),
      ),
    );
  }
}
